        <div class="user-card-title">
          <%= cl_image_tag @user.photo.key, height: 300, width: 400, crop: :fill %><h2><%= @user.nickname %></h2>
            <p><%= @user.nickname %></p>
            <p><%= @user.age %></p>
            <p><%= @user.about_me %><p>
          <%= link_to "Back", :back %>
        <a class="btn btn-flat" href="users_path">Poke</a>
